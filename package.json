{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "keywords": [],
  "license": "ISC",
  "author": "",
  "main": "app.js",
  "scripts": {
    "heroku-prebuild": "cd web && npm install && cd views && npm install && npm run build",
    "build": "cd web && npm install && cd views && npm install && npm run build",
    "start": "cd web && node app.js"
  },
  "dependencies": {
  },
  "devDependencies": {
  }
}
